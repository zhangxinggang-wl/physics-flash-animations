# Flash 物理教学动画资源库 · 使用说明

**张兴刚工作室** · 1381 个 Flash 教学动画 · 按人教版高中物理章节归档

本资源库提供**两套**使用方式，按你的场景选：

| 场景 | 用什么 | 能否播放 |
|---|---|---|
| 在线浏览（手机 / 任意电脑） | 部署到 GitHub Pages | ✅ Ruffle 在线播放 |
| 本地离线（本机） | `PlayFlash.bat` 启动器 | ✅ 本地播放器 |

---

## 一、先说清两件事

### 1. 为什么以前点卡片打不开

现代浏览器（Chrome / Edge / Firefox）**自 2020 年起完全移除了 Flash 支持**，`.swf` 文件无法被浏览器直接渲染。

以前看到的 `Forbidden` 则来自 **WorkBuddy 内置预览服务的沙箱限制** —— 该服务只托管单个 HTML 文件，访问同目录的子文件夹一律返回 403。这跟文件本身是否损坏、权限是否正常**完全无关**。

### 2. 现在怎么解决的

引入 **Ruffle** —— 一个用 Rust + WebAssembly 重写的开源 Flash 模拟器，让浏览器重新获得播放 `.swf` 的能力，**无需安装任何插件**。

---

## 二、在线使用（GitHub Pages）

访问部署后的地址（形如 `https://<用户名>.github.io/<仓库名>/`）：

- 顶部搜索框：实时按名称过滤
- 章节胶囊：快速跳转到对应章节
- **点击任意卡片 → 新标签页打开播放器**

播放页快捷键：

| 按键 | 功能 |
|---|---|
| `Backspace` | 返回目录 |
| `F` | 全屏 / 退出全屏 |
| `R` | 重新加载动画 |

部署步骤详见 `README.md`。

---

## 三、本地离线使用

### 方式 1：图形启动器（推荐）

双击 **`PlayFlash.bat`**，弹出启动器窗口：

- 左侧：按章节筛选（全部 + 18 个章节，带计数）
- 顶部：关键词搜索
- 右侧：动画列表，**双击任意一项直接播放**
- 底部：`▶ 播放选中` / `打开所在文件夹` / `打开播放器`

> 该启动器扫描的是**本地磁盘上的完整资源**（包含未上传的 113 个 `.exe` 交互实验）。

### 方式 2：直接双击 SWF

在文件资源管理器里进入任意章节文件夹，**双击 `.swf` 文件**即可播放。

本机的 `.swf` 文件关联已配置好，指向：

```
C:\Users\19368\Desktop\系列讲座\高中物理动画\flashplayer（动画播放器）.exe
```

### 方式 3：先开播放器再拖文件

打开 `flashplayer（动画播放器）.exe`，把任意 `.swf` 拖进播放器窗口。

> ⚠️ **不要用 `file://` 双击打开 `index.html` 来在线播放**：浏览器在 `file://` 协议下禁止加载 Ruffle 的 wasm 模块，在线播放功能会失效。本地请用方式 1 或 2。

---

## 四、目录结构

```
高中物理动画/
├── _按章节归档/                    ← 本目录（= 待推送的 Git 仓库）
│   ├── index.html                  ← 目录页（搜索 + 章节导航 + 1381 张卡片）
│   ├── player.html                 ← 播放页（Ruffle 加载器）
│   ├── ruffle/                     ← Ruffle 播放引擎（27.9 MB，自托管）
│   ├── README.md                   ← 项目说明与部署指南
│   ├── 使用说明.md                  ← 本文件
│   ├── PlayFlash.bat               ← 本地启动器（双击运行）
│   ├── FlashAnimationPlayer.pyw    ← 本地 GUI 播放器主体
│   ├── 00-初中物理/          (69)
│   ├── 01-直线运动/          (44)
│   ├── 02-相互作用与力/      (44)
│   ├── 03-牛顿运动定律/      (55)
│   ├── 04-曲线运动与抛体/    (63)
│   ├── 05-万有引力与航天/    (22)
│   ├── 06-机械能与功/        (36)
│   ├── 07-电场与磁场/        (76)
│   ├── 08-电磁感应/          (47)
│   ├── 09-交变电流与电磁波/  (30)
│   ├── 10-动量守恒/          (27)
│   ├── 11-机械振动与波/      (93)
│   ├── 12-光学/             (109)
│   ├── 13-热学与分子动理论/  (48)
│   ├── 14-原子物理与核物理/  (32)
│   ├── 90-实验仪器与测量/    (62)
│   ├── 91-电路与电学基础/    (50)
│   └── 99-待识别/           (474)
│
└── flashplayer（动画播放器）.exe   ← 本地播放器（未上传，见下）
```

### 关于未上传的 `.exe`

本目录共有 **113 个 `.exe` 交互实验文件，合计 295 MB**，占资源库总体积的 **74.5%**。

它们已被 `.gitignore` 排除，原因：

1. 浏览器**无法运行** `.exe`，上传后在线完全用不上
2. 体积过大（会让仓库从 101 MB 膨胀到 396 MB，上传耗时且拖慢 clone）
3. GitHub 对可执行文件有额外的安全扫描

**原始文件全部保留在本地原位**，未移动、未删除、未改名。本目录中的章节文件夹是**副本归档**。

---

## 五、关于「99-待识别」的 474 个文件

约 34% 的动画文件名属于以下形式，无法从名字判断章节：

- 纯数字编号：`001.swf` ~ `096.swf`
- P 系列编码：`PX3005.swf`、`P01-02-03.swf`（疑似"必修X-章-节"）
- 拼音缩写或无意义短名：`（二）.swf`、`钟.swf`、`青蛙.swf`、`锤子.swf`

SWF 内部文字采用 **Flash 位压缩（bit-packed）编码**，无法直接提取中文标题。

为避免误归档，这批文件统一放入 `99-待识别/`，**未做任何猜测性重命名**。

如需处理，可选方案：

1. 逐个打开截图，人工识别后重命名
2. 按文件大小 / 创建时间聚类，批量推断
3. 验证 P 系列编码规则（若确为"必修-章-节"，可自动归档）

---

## 六、常见问题

**Q：播放页显示"无法播放这个动画"？**
A：可能是该动画依赖 Flash 的高级特性（如 ActionScript 3 的部分 API）。Ruffle 对 AS2 支持最好，AS3 覆盖度仍在提升。可在本地用原播放器打开。

**Q：播放页一直转圈？**
A：首次访问需要下载 Ruffle 的 wasm（约 14 MB），网络较慢时请稍等。之后浏览器会缓存。

**Q：双击 `PlayFlash.bat` 窗口一闪而过？**
A：说明没找到带 tkinter 的 Python 解释器。改用「方式 2」直接双击 SWF 即可。

**Q：能让这些动画在手机上看吗？**
A：可以。在线版本已适配移动端，手机浏览器打开 GitHub Pages 地址即可，Ruffle 会以触摸方式操作 Flash 动画。

**Q：想让动画变成 mp4 视频？**
A：需要逐个用 Flash 导出或屏幕录制，1381 个文件工作量大。如有需要可另开任务处理。
